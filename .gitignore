# Clasp
.clasp.json

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Credentials & secrets
*.pickle
*.json.bak
creds.json
credentials.json
token.json

# V3 local runtime config (secrets: OAuth client + refresh tokens) and state.
# config.example.json IS committed; config.json is NOT.
config.json
state.json

# Outlook refresh-token stores (per-account MSA tokens). Belt-and-suspenders:
# the real tokens live in ~/.config/gmail-daily-digest, never the repo, but
# guard the default-token-dir case (= config.json dir) anyway.
outlook-token-*.json

# Environment variables
.env

# Playwright MCP
.playwright-mcp/
docs/SESSIONS.md
